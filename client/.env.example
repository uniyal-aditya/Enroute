# Base URL of the backend WITHOUT the /api suffix and WITHOUT trailing slash.
# Leave unset for local dev (Vite proxy forwards /api to http://127.0.0.1:8000).
# Example for production:
# VITE_API_BASE=https://enroute-api-production.up.railway.app
