# ==============================================================================
# ENROUTE FRONTEND (VITE + REACT) ENVIRONMENT VARIABLES
# ==============================================================================

# Backend API Base URL (without trailing slash)
# Leave blank or unset for local development (Vite automatically proxies /api to http://127.0.0.1:8000)
#
# For production (Vercel deployment connecting to Railway backend):
# VITE_API_URL=https://enroute-production.up.railway.app
